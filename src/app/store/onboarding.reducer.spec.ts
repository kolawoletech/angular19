import { onboardingReducer } from './onboarding.reducer';

describe('OnboardingReducer', () => {
  it('should create an instance', () => {
    //expect(new onboardingReducer()).toBeTruthy();
  });
});
