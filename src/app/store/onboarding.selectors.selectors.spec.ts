import { OnboardingSelectors } from './onboarding.selectors';

describe('OnboardingSelectors', () => {
  it('should create an instance', () => {
    expect(new OnboardingSelectors()).toBeTruthy();
  });
});
